---
const code = `<span class="comment">// Generic function — monomorphizes at compile time</span>
<span class="func">max</span> : <span class="keyword">func</span>&lt;<span class="type">T</span>&gt;(a : <span class="type">T</span>, b : <span class="type">T</span>) -&gt; <span class="type">T</span> &#123;
    <span class="keyword">if</span> (a &gt; b) &#123;
        <span class="keyword">return</span> a
    &#125;
    <span class="keyword">return</span> b
&#125;

<span class="comment">// Slice types — fat pointers with embedded length</span>
<span class="func">sum</span> : <span class="keyword">func</span>(s : <span class="type">[]i32</span>) -&gt; <span class="type">i32</span> &#123;
    total := <span class="number">0</span>
    <span class="keyword">for</span> x <span class="keyword">in</span> s &#123;
        total <span class="operator">+=</span> x
    &#125;
    <span class="keyword">return</span> total
&#125;

<span class="comment">// Usage</span>
arr : <span class="type">i32</span>[<span class="number">5</span>] = &#123;<span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>, <span class="number">4</span>, <span class="number">5</span>&#125;
result := <span class="func">sum</span>(arr[:])          <span class="comment">// 15</span>
biggest := <span class="func">max</span>(<span class="number">10</span>, <span class="number">20</span>)       <span class="comment">// max&lt;i32&gt; → 20</span>`;
---

<section class="py-20 px-6">
  <div class="max-w-4xl mx-auto">
    <h2 class="section-title text-3xl text-center mb-4">See Chic in Action</h2>
    <p class="text-chic-muted text-center mb-10 max-w-xl mx-auto">
      Clean, expressive syntax with powerful type inference and zero-cost generics.
    </p>

    <div class="chic-card p-3">
      <div class="code-block">
        <pre><code set:html={code} /></pre>
      </div>
    </div>
  </div>
</section>
